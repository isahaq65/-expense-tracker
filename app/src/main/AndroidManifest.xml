<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.exa.expensetracker">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/glass"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/glass"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExpenseTracker">
        <activity android:name=".LoginActivity"
        android:theme="@style/Theme.ExpenseTracker.NoActionBar"></activity>
        <activity android:name=".SearchMenu" />
        <activity
            android:name=".AccountActivity"
            android:theme="@style/profiletheme" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.ExpenseTracker.NoActionBar" />
        <activity
            android:name=".SplashScreen"
            android:theme="@style/Theme.ExpenseTracker.NoActionBar"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>